{
  "name": "lovechapter",
  "version": "0.1.0",
  "private": true,
  "type": "module",
  "workspaces": [
    "apps/*",
    "packages/*"
  ],
  "engines": {
    "bun": "1.4.2",
    "node": ">=24"
  },
  "scripts": {
    "build": "npm run build --workspace @lovechapter/api && npm run build --workspace @lovechapter/jobs && npm run build --workspace @lovechapter/web",
    "check": "npm run format:check && npm run lint && npm run typecheck && npm test && npm run build",
    "ci": "npm run check && npm run db:check --workspace @lovechapter/database && npm run smoke:bun --workspace @lovechapter/api && npm run build:worker --workspace @lovechapter/api && npm run build:next --workspace @lovechapter/web && npm run check:vinext --workspace @lovechapter/web && npm run deploy --workspace @lovechapter/web -- --dry-run",
    "format": "prettier --write .",
    "format:check": "prettier --check .",
    "lint": "eslint .",
    "test": "vitest run",
    "test:watch": "vitest",
    "typecheck": "npm run typecheck --workspaces --if-present"
  },
  "devDependencies": {
    "@eslint/js": "10.0.1",
    "@types/node": "26.6.2",
    "eslint": "10.11.0",
    "prettier": "3.9.8",
    "prettier-plugin-tailwindcss": "0.8.1",
    "typescript": "6.0.3",
    "typescript-eslint": "8.70.0",
    "vite": "8.3.0",
    "vitest": "5.0.1"
  }
}

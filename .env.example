# Direct PostgreSQL connection used only by Drizzle Kit migrations. Use a
# separate least-privilege migration role; never reuse or commit production
# credentials.
DATABASE_URL=postgres://migration_user:replace_me@db.example.test:5432/lovechapter?sslmode=require

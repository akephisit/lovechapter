# Production uses the verified HTTPS API hostname; these values remain
# server-only and must never use a NEXT_PUBLIC_ prefix.
API_UPSTREAM_ORIGIN=http://127.0.0.1:3001
WEB_PROXY_SHARED_SECRET=AAAAAAAAAAAAAAAAAAAAAAAAAAAAAAAAAAAAAAAAAAA
